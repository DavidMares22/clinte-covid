<page loaded="onPageLoaded">


<!-- <ActionBar title="SCAE" class="text-center"></ActionBar> -->
<ActionBar>
<Label text="SCAE" width="100%" class="h2 text-center" />
</ActionBar>

    <StackLayout>
        <!-- <Label text="{{codigo}}" width="100%" class="h2 text-center" backgroundColor="yellow"/> -->
        
        <Button class="btn" text="Ver codigo QR" tap="goQR" ></Button>
        <Button class="btn" text="Comunicar diagnóstico" tap="goContact" ></Button>
        <Button class="btn" text="Historial" tap="goToHistory" ></Button>

            <Label text="Notificaciones de posible contagio" width="100%" class="h3 text-center"/>
        <ActivityIndicator busy="{{IsBusy}}" color="red" width="100" height="100" visibility="{{ IsBusy? 'visible' : 'collapsed' }}"></ActivityIndicator>
        <ListView items="{{taskList}}"  orientation="vertical" height="650" >
          <ListView.itemTemplate>
              <Label text="{{title}}" color="white" horizontalAlignment="center" verticalAlignment="center" backgroundColor="red"/>
          </ListView.itemTemplate>

        </ListView>
        <!-- <Button text="remover almacenamiento" class="bg-danger" tap="removerCodigo" ></Button> -->
    </StackLayout>
</page>

